import styled from "styled-components";

const OverlayDiv = styled.div`
  position: absolute;
  color: white;
`;

export const ControlsOverlay = () => {
  return <OverlayDiv>Controls</OverlayDiv>;
};
